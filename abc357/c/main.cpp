#include <bits/stdc++.h>

using namespace std;
vector<vector<int>> n;
vector<int> makesierpinski(vector<vector<int>>& n, int k){
	
}

};
int main() {
	int n;
	cin >> n;
	vector<vector<int>> table(n, vector(n, 1));
	return 0;
}
